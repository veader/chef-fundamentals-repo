---
driver:
  name: vagrant
  provider: vmware_fusion

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.5

suites:
  - name: default
    run_list:
      - recipe[apache::default]
    attributes:
      apache:
        sites:
          ponies:
            port: 8082
